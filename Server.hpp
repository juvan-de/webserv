#ifndef SERVER_HPP
# define SERVER_HPP

# include <string>
# include <iostream>
# include <fstream>
# include <vector>
# include <map>

class	Server
{
	private:
		std::string;

	public:
		Server();
		Server(const Server& ref);
		Server& operator=(const Server& ref);
		~Server();


};

#endif
